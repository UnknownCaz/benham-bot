# Benham — Owner's Guide

**The manual moved.** It's now a local site: open **`docs/index.html`** (double-click it, or
tray icon → Open manual). Outer = how to drive it; Inner = why it's built this way.
This file is a pointer, kept so old links don't dangle. Update the site, not this file.
