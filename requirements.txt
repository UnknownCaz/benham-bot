discord.py>=2.3
python-dotenv>=1.0
PyNaCl>=1.5
davey>=0.1
# Voice listening (receive+STT). Blocked on discord.py 2.7 by DAVE E2E encryption;
# intended for a future pre-DAVE (discord.py 2.4.x) venv. See bot.py note.
discord-ext-voice-recv>=0.5
faster-whisper>=1.0
# Autonomous AUTO_REPLY mode (bot answers via the Anthropic API itself)
anthropic>=0.40
# Natural neural TTS (replaces Windows SAPI)
edge-tts>=7.0
