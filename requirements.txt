python-dotenv>=1.0

# Discord. Pinned to the 2.7 line, which is what every gateway/reference/reply
# behaviour in bot.py and test_pc_reply.py was verified against.
discord.py==2.7.*

# The agent, the guest brain, and the pc.. relay all talk to the Anthropic API.
anthropic>=0.40

# exaroton /server slash commands + watchdog (exaroton_ops.py; also used by the
# exaroton skill).
requests>=2.28

# Voice was archived 2026-08-16 and took six dependencies with it: PyNaCl,
# discord-ext-voice-recv, davey, faster-whisper, numpy and edge-tts. That trio of
# voice-recv/davey/discord.py was version-locked around a monkeypatch of a PRIVATE
# symbol - see archive/voice/README.md before reinstating any of them.
